package org.jnbt;

public abstract class ValueTag extends Tag{
    /**
     * Creates the tag with the specified name.
     *
     * @param name The name.
     */
    public ValueTag(String name) {
        super(name);
    }
}
